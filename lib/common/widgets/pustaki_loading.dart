import 'package:flutter/material.dart';

class PustakiLoading extends StatelessWidget {
  const PustakiLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
