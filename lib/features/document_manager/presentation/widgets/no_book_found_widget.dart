import 'package:flutter/material.dart';

class FetChDocumentEmptyDataWidget extends StatelessWidget {
  const FetChDocumentEmptyDataWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No book found"),
    );
  }
}
